# F237: adaptive racing — parity at lower cost (2026-08-13)

rb/  raced_battery.py, 6 seeds: flat (min-of-two-streams) vs raced
     (CRN staged racing), identical candidate spaces. Parity on eval
     (t=+0.09), 0.86x episodes, optimism unchanged at ~+0.2 (residual
     = inter-stream variance, not candidate-count bias).

Narrative: docs/MEMORY_BANK_DESIGN.md F237.
