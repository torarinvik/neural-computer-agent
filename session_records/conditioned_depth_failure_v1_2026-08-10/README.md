# Conditioned depth failure (F134)

Probe 234, the clean test. Boolean pieces (no grokking confound),
oracle entries (reading removed), iterated interface (the one that
scores 1.0000 at one world), 256 worlds, depth<=4:

  own 0.5587 per-bit | stranger 0.5439 | withheld 0.5043 | chance 0.5
  single-world control: 1.0000

The plant uses the entry (own > stranger > withheld) but cannot
execute with it. Pieces from weights: any depth. Pieces from context:
depth 1 only. The bank thesis needs both.
