# Three-program external capability bank — 2026-08-06

Status: promoted narrow two-seed three-program capacity result.

This is the next rung after the two-file bank. A span-2 parent controller is
frozen while three independent memory-side recurrent programs are acquired
from fresh rendered events, opaque actions, and scalar verifier outcomes:

- `reverse4`
- `forward4`
- `complement4`

Each opaque file contains its recurrent context program and capability-local
output decoder. A permutation-equivariant learned router selects one file from
the standardized event signature. The shared controller, frontend, and parent
output path are unchanged.

Both seeds pass every gate:

- stable-prefix program mastery: `14,336/8,192/12,288` and
  `10,240/6,144/6,144` verifier bits;
- selected accuracy: `0.895/0.973/0.898` and `0.934/0.961/0.992`;
- wrong-program accuracy: `0.566/0.094/0.406` and `0.539/0.047/0.473`;
- learned route, permutation, and reloaded route accuracy: `1.000`;
- balanced reward-shuffled route control: `0.000` and `0.083`;
- parent retention, frozen-core digest, exact candidate reload, and corruption
  rejection all pass; replay count is zero.

The archived `artifact_bank_seed69317/` and `router_seed69317.pt` provide one
verified persistent payload. Reports contain full histories, accounting, and
all gates for both seeds.

This promotes bounded three-file memory growth, not general continual
learning. The next blocker is sequential append under capacity pressure:
eviction/consolidation, route reversal after nonstationarity, and retaining
all earlier programs while new files are learned without replay.
