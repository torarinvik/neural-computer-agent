# Multi-order held-out composition — rejected promotion

The four learned opaque instructions were reused across three new orders.
Seed `69316` transferred all three at the 128-update composition rung:
inherited stable costs were `8,192`, `4,096`, and `4,096` bits versus fresh
costs of `12,288`, `12,288`, and `16,384`. Seed `69317` transferred two of
three; its first order tied fresh at `8,192` bits. Doubling only composition
updates for seed `69317` preserved that tie while the other two orders still
transferred.

All source/mastery, retention, shuffled-outcome, missing-evidence, reload,
corruption, frozen-parent, and zero-replay controls passed. The all-target
replicated promotion gate is nevertheless rejected. This is partial evidence
for reusable multi-order composition, with order-robust transfer calibration
remaining open.
