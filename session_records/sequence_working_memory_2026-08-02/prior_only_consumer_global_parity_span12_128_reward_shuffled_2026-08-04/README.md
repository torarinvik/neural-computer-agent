# Reward-shuffled producer→consumer control — rejected

The same prior-only recurrent consumer was trained with scalar outcomes
shifted across batch rows. Composed accuracy stayed at `48.70%`, with parent,
producer-only, and consumer-only controls all near chance. The causal
composition gates failed, as required; this control was not promoted.
