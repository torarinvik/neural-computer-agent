# Page-local 46-candidate source boundary rejected (2026-08-07)

The promoted page-local representation boundary was pushed from 26 to 46
opaque candidates: 20 source candidates and 26 unseen candidates across 13
isolated append pages. Both seeds retain perfect unseen acquisition and
permutation, but known-source routing is `0.9271` with a per-target floor of
`0.4000` on both seeds. Strict source mastery therefore rejects this rung.

Normalization increases candidate-key effective rank to `11.90/12.87`, so the
failure is not solved by making signatures more linearly separable. It
localizes the next bottleneck to source-screen capacity/interference at 20
protected rows. No controller weights were changed and replayed examples were
zero.
