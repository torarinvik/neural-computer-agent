# Generated composition pipeline pilot rejection (2026-08-06)

Status: rejected as under-budget; no weights promoted.

A two-program `ExternalCapabilityPipeline` was tested on the same
verifier-private generated composition workload. It reached `0.6406` held-out
behavior after 64 updates, slightly above the monolithic pilot's `0.6094`, but
never reached a stable `0.75` prefix. The parent was stable and unchanged;
replay was zero.

This short result does not establish or reject the compositional mechanism.
The next rung uses the established 256-update budget.
