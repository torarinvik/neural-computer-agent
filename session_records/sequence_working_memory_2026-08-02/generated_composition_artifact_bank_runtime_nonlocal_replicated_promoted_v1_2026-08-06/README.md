# Replicated nonlocal runtime-grammar acquisition (2026-08-06)

Status: replicated promoted temporal/aggregation mechanism-transfer result.

The runtime-private composition grammar was extended beyond reversible
pointwise primitives. The audit supplied these four four-primitive programs:

1. `reverse -> adjacent_xor -> complement -> prefix_parity`
2. `prefix_parity -> global_parity -> rotate -> complement`
3. `global_parity -> reverse -> adjacent_xor -> rotate`
4. `complement -> prefix_parity -> reverse -> global_parity`

`adjacent_xor`, `prefix_parity`, and `global_parity` now use the same generic
runtime program interface as reverse, complement, and rotation. The controller
still receives only rendered learned events; the program grammar remains
verifier-private. Each artifact was acquired in an isolated external stack,
admitted after stable fresh retention outcomes, and routed through the frozen
append-only chain.

| metric | seed 69316 | seed 69317 |
| --- | ---: | ---: |
| nonlocal artifact 0 behavior | 0.9336 | 0.9570 |
| nonlocal artifact 1 behavior | 1.0000 | 1.0000 |
| nonlocal artifact 2 behavior | 1.0000 | 1.0000 |
| nonlocal artifact 3 behavior | 1.0000 | 1.0000 |
| causal route accuracy | 1.0000 | 1.0000 |
| candidate-key permutation accuracy | 1.0000 | 1.0000 |
| cold-start old-route accuracy | 1.0000 | 1.0000 |
| stage-specific shuffled controls | 0.0000 | 0.0000 |

All artifact rows were stable and protected. Reload, corruption rejection,
frozen-core, and zero-replay gates passed in both runs. A short underpowered
rung correctly refused the first append because the candidate was not yet
protected; the full acquisition budget was retained for the promotion.

This promotes replicated composition of temporal and aggregation procedures
through the runtime grammar and frozen append-only memory boundary. It remains
bounded external growth: the artifact blueprint and append-only capacity are
finite, and arbitrary open-ended program induction or general continual
learning is not established.
