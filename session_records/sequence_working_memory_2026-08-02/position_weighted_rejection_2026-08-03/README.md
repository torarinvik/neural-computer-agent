# Outcome-only position weighting rejection (2026-08-03)

The first outcome-only position-difficulty weighting arm was rejected by the
corrected audit. It appeared to acquire span 11 at 256 lifetimes
(+2.38 pp), but the child blank-sequence control fell to **34.87%**. The
hard missing-evidence gate therefore rejects the arm despite positive causal
gain and nominal retention. This is a shortcut/control failure, not a
capability promotion.

The run charged 384 unique lifetimes and 3,840 verifier bits, with 960
optimizer updates. The checkpoint remains in `/tmp` only and is intentionally
not curated. The training and corrected audit receipts are stored beside this
record.
