# Promoted verifier-gated transition-model capacity growth

Two seeds (`70011`, `70012`) extend the online alternating-context rung from
safe capacity refusal to verified external growth. A fourth regime was first
presented at capacity three and refused without writing or modifying the
existing slots. A caller-owned held-out retention probe then passed for the
base, auxiliary, and target regimes; `grow_verified` increased capacity to
four without changing the bank content digest; and the fourth regime was
admitted and reached `1.0` measured mastery.

Earlier regimes retained `1.0`, the controller received zero updates, old
prior rows were never replayed during target learning, and wrong-context,
corruption, fresh-model, and exact-persistence controls passed in both seeds.

This promotes capacity growth only. The system still refuses consolidation of
factually distinct transition functions until behavior-preserving equivalence
is demonstrated; compression and long-horizon alternating capacity pressure
remain the next frontier.
