# Learned opaque address discovery replica — 2026-08-04

Independent seed `69415` replicated the factorized learned-address route.
Routing reached `100%`, candidate permutation remained `100%`, and the
reward-shuffled control stayed at `33.3%` chance. Selected execution reached
span 2 `100%`, span 3 `97.92%`, and span 4 `87.11%`.

Stable span-4 acquisition required `12,288` verifier bits. Capability and
route training used zero replayed examples, and the frozen parent core was
bit-identical. This remains a narrow external-artifact retention result, not
general address discovery or arbitrary lifelong learning.
