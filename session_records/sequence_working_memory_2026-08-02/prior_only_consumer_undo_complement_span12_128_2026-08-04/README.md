# Prior-only consumer span-12 control — rejected

Repeating the undo-complement probe at span 12 still encountered parent
saturation: composed `70.70%` versus parent `75.00%`. The producer and prior
read were causal as pathways, but the chain added no new capability. It was
not promoted or scaled.
