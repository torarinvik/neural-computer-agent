# Generated composition pilot rejection (2026-08-06)

Status: rejected as under-budget; no weights promoted.

The short ladder rung trained one fresh external recurrent capability on the
verifier-private two-primitive composition workload. The frozen parent was
mastered and unchanged, but the generated capability reached only `0.6094`
held-out behavior after 64 updates and never reached a stable `0.75` prefix.

This is an acquisition-budget diagnostic, not evidence that the generated
composition mechanism fails. The next rung uses the established 256-update
capability budget before any route-isolated continual-learning promotion.
Replay was zero; the raw report is `report_seed69316.json`.
