# Learned binding with verified factual consolidation

This archive records the promoted compaction rung after external memory
growth. Three anonymous tracks address three factual slots in one shared bank.
Two slots contain equivalent transition computation but retain separate opaque
stream identities; the third uses a different random-feature family.

Across seeds 2701, 2702, and 2703:

- the learned encoder separated all three anonymous tracks;
- a distinct model-family consolidation candidate was rejected atomically;
- a retention probe that attempted to mutate candidate state was rejected
  atomically;
- held-out-equivalent affine slots shared physical parameters from three model
  objects to two;
- all three stable factual slot addresses and binding keys remained intact;
- exact persistence, frozen controller/encoder, zero replay, and zero factual
  optimizer updates passed.

The raw reports are report_seed2701.json, report_seed2702.json, and
report_seed2703.json. sample_efficiency_ledger.json records the required
accounting and SHA256SUMS protects the archive.

## Claim boundary

This promotes bounded retention-safe external parameter sharing. It does not
establish learned consolidation policy, unrestricted memory growth, semantic
stream merging, arbitrary new computation, or general continual learning.
