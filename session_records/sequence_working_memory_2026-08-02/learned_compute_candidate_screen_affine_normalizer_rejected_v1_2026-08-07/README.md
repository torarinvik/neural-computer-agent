# Affine signature normalization rejected (2026-08-07)

This control fits a frozen permutation-invariant affine transform from the
opaque candidate-key set and applies it identically to queries and keys at the
bank-26/six-stage/1024-update/full-prior boundary.

The transform raises effective rank and fixes source mastery on both seeds,
but it trades away unseen acquisition on the hard seed: unseen routing is
`1.0000/0.8854`, with one target at `0.0`. The raw representation's unseen
path was perfect at this rung. The normalizer is therefore not promoted as a
global replacement; its versioned contract remains available for page-local
alignment experiments.
