# Two-artifact generated composition bank: control variance (2026-08-06)

Status: not promoted; the mechanism passed positive gates but the control
design was not yet strong enough for a promotion claim.

Two independently acquired generated-composition artifacts reached behavior
`1.0000` and `0.9453`. Both rows were protected, causal routing reached
`1.0000`, permutation routing reached `1.0000`, reload preserved routing,
corruption was rejected, the parent stayed frozen, and replay was zero.

The single reward-shuffled router control scored `0.75` on the first audit and
`0.73–0.81` across a stronger four-audit rerun. Independent shuffled-router
repeats scored approximately `0.78`, `0.67`, and `0.19`, showing high variance
from the two-row control rather than a reliable negative result. No promotion
is made. The audit must replicate shuffled controls inside the report and use
their distribution, not one noisy score, before claiming causal routing.
