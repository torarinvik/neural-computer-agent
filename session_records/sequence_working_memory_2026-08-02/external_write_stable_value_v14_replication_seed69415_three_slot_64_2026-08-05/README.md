# Stable controller value path — seed 69415 at 704 phase-1 steps

Status: rejected as an insufficient curriculum budget, not as a capability
failure. The parent reached high final diagnostic recall but did not satisfy
the required three consecutive stability audits, so retention was correctly
blocked. No retention updates were counted.

The smallest tested extension, phase 1 `800`, passed and is recorded in the
sibling `phase1_800` directory.
