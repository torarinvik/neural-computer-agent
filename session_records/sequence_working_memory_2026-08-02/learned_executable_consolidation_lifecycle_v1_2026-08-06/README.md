# Lifecycle-backed learned executable consolidation (2026-08-06)

This is the canonical rerun of learned executable-artifact consolidation
through `ExternalCapabilityLifecycle`. Four independently acquired executable
artifacts are compressed from four physical rows to one through three
retention-gated, behavior-verified rewrites. The controller, artifact payloads,
and earlier retained views remain protected while the memory-side proposal
policy selects each pair.

The final compacted bank is then routed by an opaque address learner using
fresh paired scalar outcomes. The default route budget is now 2,048 updates
per arm (4,096 including the balanced shuffled control); the lower 512-update
budget is retained as a rejection control because it is not reliably above the
permutation gate on this four-view bank.

## Promoted result

| metric | seed 69316 | seed 69317 |
| --- | ---: | ---: |
| route accuracy | 1.0000 | 0.9453 |
| candidate permutation | 1.0000 | 0.9453 |
| shuffled route accuracy | 0.2773 | 0.2500 |
| physical rows after compaction | 1 | 1 |
| replayed examples | 0 | 0 |
| controller updates during consolidation | 0 | 0 |

Both seeds pass independent artifact mastery, three sequential compactions,
fresh retention protection at every step, behavior preservation after reload,
wrong-view causal separation, checksum-corruption rejection, exact view
reload, frozen-core equality, and route permutation invariance.

The route-512 seed-69317 control is stored as
`rejected_route_budget_seed69317.json`: compaction and behavior passed, but
route/permutation accuracy was only `0.8828`, so the complete result was not
promoted. This is a useful acquisition-depth signal, not a hidden failure.

## Claim boundary

This promotes a reusable lifecycle-backed executable-memory transaction and
identifies the minimum route-acquisition budget for this bank. It remains
bounded continual memory: executable artifacts are externally acquired, the
verifier gates consolidation, and the route learner does not synthesize an
arbitrary new program. Open-ended nonstationary growth, robust positive
transfer, byte-level compression, and general continual learning remain open.
