# Multi-stage append-only learned screen growth (2026-08-07)

This promoted experiment tests sequential isolated growth of the learned
compute-candidate screen. Six mastered candidates remain in a copied,
frozen base; four outcome-unseen candidates are split across two independent
two-candidate extensions. A later extension can compete only after the base
and every earlier extension have supplied scalar verifier failure.

Across seeds `69316` and `69317`, known-candidate novel-context routing is
`1.0000`. Unseen routing is `0.0000` before activation and `1.0000` after the
cumulative failure schedule. Base and stage-local candidate permutations,
exact reload, frozen-core, reward-shuffled null, and zero-replay controls all
pass.

This promotes replicated multi-stage bounded external growth and the
cumulative activation contract. It does not establish unbounded memory,
arbitrary new computation, open-ended compression, or general continual
learning. Full accounting is in `sample_efficiency_ledger.json`; report
checksums are in `SHA256SUMS`.
