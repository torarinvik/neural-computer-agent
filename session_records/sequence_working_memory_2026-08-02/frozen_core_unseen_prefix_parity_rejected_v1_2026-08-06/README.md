# Unseen prefix-parity external growth — 2026-08-06

Status: retained as a positive single-seed capability diagnostic, rejected as
a replicated continual-transfer promotion.

`prefix_parity` was not part of the earlier forward/reverse/complement
transfer set. A frozen controller learned it through the same opaque
event/action/outcome boundary and the same external recurrent growth blueprint,
with fresh forward rehearsal and zero replay.

| arm | transferred stable bits | fresh stable bits | transfer ratio | target | retention | shuffled |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| direct seed 69316 | 12,288 | 18,432 | 1.50× | 0.8984 | 1.0000 | 0.4297 |
| direct seed 69317 | 24,576 | 24,576 | 1.00× | 0.7656 | 0.9844 | 0.6328 |

This establishes the important narrower result: the generic external blueprint
can acquire an unseen temporal computation without changing the controller.
It does not establish replicated positive transfer. A second-parent control
initially produced `1.60×` transfer but forgot the extra parent task
(`0.5000` retention). The corrected multi-stream rehearsal trainer preserved
that extra parent on seed 69316 but lost transfer (`0.857×`), while seed 69317
still forgot the first parent (`0.6250`). Equal-loss rehearsal therefore does
not solve multi-skill interference.

The next repair is route-isolated growth: keep new computation in an independent
external artifact/decoder path so it cannot perturb an older output residual.
All reports retain verifier bits, logical lifetimes, optimizer updates, wall
time, replay count, and causal/shuffle controls.
