# Learned executable-artifact route acquisition — 2026-08-06

Status: promoted outcome-trained opaque address acquisition over a compacted
executable-artifact bank.

This rung starts from the promoted four-artifact, one-row compaction protocol.
After three retention-gated rewrites, a permutation-equivariant opaque router
is trained from attempted-row outcomes while the controller and executable
artifacts remain frozen. The selected router index is converted to an opaque
alias key and resolved through the generic artifact-memory promotion path
before the view is executed.

Both canonical seeds 69316 and 69317 pass:

- route accuracy and candidate-permutation accuracy exceed 90%;
- reward-shuffled routing remains near chance;
- all four routed procedures pass the `.75` behavior gate;
- wrong-view behavior is causally lower for every procedure;
- route and behavior survive persistent reload;
- the four-view retention-safe compaction chain, frozen core, and corruption
  controls remain passing;
- replayed examples and controller updates during consolidation remain zero.

The 128-update route smoke is rejected by the route/permutation gate. This
promotes bounded learned address acquisition over executable external memory,
not arbitrary skill induction, byte compression, unrestricted memory growth,
or general continual learning.
