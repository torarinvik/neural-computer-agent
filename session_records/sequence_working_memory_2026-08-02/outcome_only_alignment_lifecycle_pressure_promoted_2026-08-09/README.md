# Outcome-only alignment lifecycle pressure — promoted bounded result

This two-seed audit filled a capacity-four external alignment bank, protected
all four mastered cells from scalar outcomes, exposed cell 3 to a changed
event space until four reversal outcomes released it, and then replaced it
under the same logical slot. The three protected cells and the frozen source
computation remained intact.

Both seeds passed:

- all four base cells mastered and shuffled controls rejected;
- all four base keys became protected;
- four scalar reversal observations released only the stale cell;
- the retention ledger selected exactly that cell for eviction;
- the replacement cell mastered, its shuffled control failed, and key routing
  remained perfect across the retained cells plus replacement;
- protected-cell retention, source retention, and frozen parent/register/
  decoder digests passed;
- replayed examples: `0`.

The reversal threshold is `0.75`, below the mastery threshold `0.8`. An
earlier `0.70` setting safely refused seed 69317 because its stale outcomes
included `0.703` and `0.734`; that failure is retained as evidence that
reversal hysteresis must be calibrated to verifier noise rather than guessed.

This promotes bounded verifier-gated eviction and copy-on-write replacement.
It does not establish unrestricted memory growth, learned consolidation,
arbitrary drift recovery, or general continual learning.
