# Current online discovery rerun

This is a fresh 24-seed rerun of the current committed online goal-conditioned
discovery path. It uses the recency/latest external state boundary, the
provisional write firewall, preferred post-promotion recovery, and explicit
fresh-planner acquisition cost. The controller, rendered frontend, and
keypress decoder remain frozen.

Fourteen of twenty-four runs passed every gate: opaque target discovery,
held-out model-family verification, recursive target rollout, source-memory
retention, target route recovery, goal admission/use, and improvement over the
matched fresh planner. Source retention passed in all 24 runs. The run consumed
690 unique verifier bits, 206 logical lifetimes, and 576 transition rows once;
replay and controller changes were both zero.

The result supersedes neither the earlier 9/24 ledger nor its accounting: the
historical run is retained for provenance. The discrepancy is itself useful:
the route is not yet deterministic enough for a single aggregate claim. The
current rejected seeds were `80, 82, 83, 86, 88, 90, 93, 94, 96, 98`.

Claim boundary: this is a current-code bounded online external-memory result,
not general continual learning, arbitrary computation, unrestricted growth,
or universal positive transfer.
