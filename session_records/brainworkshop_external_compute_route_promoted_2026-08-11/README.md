# Promoted content-addressed external compute-file routing

This record promotes the two-seed rendered Brain Workshop route-discovery rung
from `experiments/brainworkshop_canonical/external_compute_route.py`.

The source `symbol_parity` file is calibrated under cue `7` and protected
before the `triplet_parity` file is appended under cue `8`. A
`PersistentOpaqueContextRouteEvidence` table stores only normalized learned
event keys, opaque slot attempts, and scalar episode outcomes. Unknown cue `9`
uses conservative append-order fallback.

Seeds `17` and `18` pass direct and routed mastery, exact source/target route
selection, unseen-cue fallback, no-file chance control, persistence reload,
protected source context, immutable external files, frozen controller/frontend,
and zero replay. This is cue-conditioned external route discovery, not
arbitrary program induction or general continual learning.
