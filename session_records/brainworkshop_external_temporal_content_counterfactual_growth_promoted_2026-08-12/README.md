# Outcome-only related-key content retrieval

This promoted bounded rung composes paired counterfactual capability
acquisition with the canonical `ExternalTemporalAddressIndex`. Eight opaque
candidate temporal addresses are searched; exactly offset `4` is admitted.
The promoted file is frozen while a scalar-outcome route ledger acquires
offset `5`. The content index is then written from the learned opaque route
positions and learned context keys only.

Seeds `17`, `18`, and `19` pass all `23/23` gates. Exact and 20%-related source
and target keys retrieve the correct positions at `1.0000` retained accuracy;
unknown-key miss, reload, checksum corruption rejection, clear, frozen
controller/event encoder/file, and zero replay all pass.

Per seed: `166,912` unique verifier bits, `336,896` counterfactual-arm bits,
`16,704` unique logical lifetimes, `33,792` counterfactual logical lifetimes,
`1,024` optimizer updates, `72` route-memory updates, two content-memory
writes, and zero replay.

This is bounded related-key external retrieval plus isolated capability growth,
not unrestricted memory growth, arbitrary new computation, program induction,
or general continual learning. Raw reports are `seed-17.json`, `seed-18.json`,
and `seed-19.json`.
