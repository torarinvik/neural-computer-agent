# Reader capability (F138)

Probe 238. Reader distilled onto the oracle entry, then task
performance measured through the frozen plant with READER entries on
HELD-OUT worlds: 0.9723 / 0.9478 per-bit (oracle ceiling 0.9983),
stranger 0.5488 / 0.5936.

Same reader, same inputs, same frozen plant: chance under task loss,
near-ceiling under distillation. The missing piece is the training
SIGNAL. Caveat: the distillation target is privileged, so this is a
capability result, not a solution. Next: contrastive reader
pre-training, which needs only same-world/different-world identity.
