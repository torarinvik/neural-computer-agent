# Promoted stochastic multi-edit control-flow frontier

This audit extends the typed external CPU/files boundary beyond one-edit
scaffold adaptation. A protected clear-loop file and a same-ABI fresh file
search with a generic replace/insert/delete/swap frontier. The frontier keeps
only scalar outcome credit and qualified provisional programs; it does not
receive the private target program or persist verifier rows.

Across seeds `17`, `18`, `19`, and `20`, with forward and reversed verifier
order, the useful root acquired a two-edit transfer program in `84`, `92`,
`193`, or `149` candidate evaluations, respectively. Every arm reached
`1.0000` held-out target accuracy and `1.0000` source retention. Missing
evidence, shuffled feedback, corrupted memory, frontier reload, memory reload,
zero replay, and zero optimizer updates all passed.

The fresh same-ABI root exhausted its qualified frontier after `50` candidate
evaluations in every arm without reaching the target. This is recorded as
`frontier_exhausted`, not `inexpressible`; the stochastic frontier did not
certify the complete program space. Because the fresh arm did not reach a
common mastery threshold, no warm/fresh transfer ratio is claimed. The
promotion is therefore for multi-edit structural acquisition, not sample
efficiency transfer.

This remains a bounded external control-flow result. It does not establish
efficient arbitrary program synthesis, unbounded deployment execution,
unrestricted memory growth, or general continual learning. The typed result
still needs a replaceable adapter across the canonical opaque intention bus.

Raw report: `report_seed-17-20.json`.
