# Math twins proving ground (F114)

Probe 214. `math_twins.py`: worlds x -> (a*x+b) mod 16, twin negates b.
Reader (8 example pairs -> entry) + plant (x, entry -> next), ignorance
0.5, pair-held-out, four controls. 2 seeds, ~2 min each.

Own 0.41/0.50 (chance 0.0625), twin 0.0000/0.0000 (plant commits to
the opposite rule — perfect sign-split), withheld ~chance, twin cosine
0.60/0.44. With dense supervision the reader sign-splits; the games'
polarity failure therefore points at the sparse value-training signal,
not reader capacity.
