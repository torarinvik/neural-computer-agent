# v57 recall-only acquisition diagnostic

v57 applies the recall-only fixed-write intervention only during initial
parent acquisition and restores ordinary outcome-only rehearsal during
retention. The parent remains stable and mastered-parent retention returns to
`1.000`, but the first two held-out validations pass and later validations
collapse, so the stable-prefix rule rejects the run. No transfer or capability
claim is made.
