# Full-information four-file route credit — promoted bounded result

This audit exercised `ControlFlowProgramAmodalRuntime` with four protected
generic external counter-machine files, a frozen amodal controller, and the
replaceable `ExternalControllerTrajectoryQueryAdapter`. For each fresh
lifetime, the verifier evaluated all four files and sent the external router
an outcome vector. The controller received quiet feedback, performed no
optimizer updates, and no old examples were replayed.

Across four seeds and both forward/reversed physical-file orders, all eight
verifier arms reached `1.0000` held-out accuracy on amplitude-2 events. Fresh
accuracy was `0.2500` in every arm. The paired independent-random-outcome
nulls were `0.1250`, `0.1250`, `0.2500`, and `0.2500`, all within the
predeclared `+/-0.15` chance band around the four-file floor.

Controller parameters stayed byte-identical, all four files remained
protected and checksummed, missing evidence was an exact external no-op, and
reload/corruption controls passed. The audit accounts for `12,000` fresh
verifier outcomes per arm, `3,000` logical lifetimes, zero replay, and zero
controller optimizer updates.

This promotes bounded full-information outcome-only routing among four
generic external files with an opaque trajectory address. It does not
establish learned codec adaptation, arbitrary new computation, unrestricted
memory growth, or general continual learning.

The machine-readable report is `report_seed-17-20.json`.
