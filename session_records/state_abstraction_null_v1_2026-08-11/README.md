# State abstraction null (F149)

Probe 249, 2 seeds paired against F143. Two nearest objects per plane
instead of one: +0.1108 / +0.1230 (mean +0.1169) against +0.0980 /
+0.1334 (mean +0.1157). One seed up, one down, flat overall.

F109's verdict survives the re-test it deserved. Three candidates for
the games residual now eliminated: value model, search budget, state
abstraction. What remains is the search's structure — specifically the
frozen-object rollout, which lets a plan count the same food twice.
