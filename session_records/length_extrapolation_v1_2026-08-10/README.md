# Length extrapolation (F125)

Probe 225. Iterated plant trained on programs of length <=3 only;
every length-4 program held out. Result 1.0000 on all 16 unseen
length-4 programs (chance 0.0435). Rules out within-length
interpolation as an explanation for F121.

Settled: a shared per-element step composes, and extends to unseen
depths. Not settled: whether the pieces can come from a BANK ENTRY —
every composition success so far is single-world.
