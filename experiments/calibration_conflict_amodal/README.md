# Outcome-only source-trust calibration

Each trajectory contains two contradictory candidate streams and one hidden
stable reliable source. The first sampled action is necessarily uncertain;
later actions can reuse only the previous opaque action and scalar verifier
outcome to infer which source was reliable. Promotion requires post-calibration
reuse, order invariance, and collapse when outcome feedback is withheld or
shuffled.
