# Operating-system and editor metadata.
.DS_Store
.idea/
.vscode/
*~

# Python environments, caches, coverage, and packaging output.
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/
*.egg-info/
.ipynb_checkpoints/

# Disposable training products. Curated milestones are force-added explicitly
# under artifacts/checkpoints or artifacts/candidates.
*.pt
*.pth
*.bin
*.idx
!artifacts/candidates/core263_parent.pt

# Local runtime products.
*.log
*.ppm
videos/
runs/
wandb/
/experiments/archive/unified_cognitive_controller/reports/

# Session-local experiment sweeps. Selected evidence reports may be
# force-added alongside promoted milestones.
/session_records/*/raw/
# Nested session sweeps (session_records/<run>/<arm>/raw/). Selected evidence
# reports are force-added alongside promoted milestones.
/session_records/*/*/raw/

# Scratch libraries grown by curriculum diagnostics. The curated bank lives in
# artifacts/checkpoints and is never written by these runs.
/artifacts/scratch/
